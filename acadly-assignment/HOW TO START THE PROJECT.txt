On first terminal (for frontend react-typescript);

npm start 
------------------------------------------------------------------------------------
on second terminal (for backend database(JSON-SERVER))

json-server --watch db.json --port 3004